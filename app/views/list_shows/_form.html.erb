<%= form_for(@list_show) do |f| %>
  <% if @list_show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list_show.errors.count, "error") %> prohibited this list_show from being saved:</h2>

      <ul>
      <% @list_show.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :show_id %><br />
    <%= f.number_field :show_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
